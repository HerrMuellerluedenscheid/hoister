# Deps are installed inside the image by `npm ci`; copying the host tree over
# them would clobber that (and defeat layer caching).
node_modules

# Build output — regenerated by `npm run build` in the builder stage.
build
.svelte-kit

.git
.env
.env.*
!.env.example
Dockerfile
.dockerignore
